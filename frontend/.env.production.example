# Template only — do not fill this in and commit it. Copy these three keys
# into Vercel's dashboard: Project Settings -> Environment Variables
# (scope: Production), using your actual deployed backend domain.

NEXT_PUBLIC_ELMODE_API_URL=https://api.yourdomain.com
NEXT_PUBLIC_ELMODE_WS_URL=https://api.yourdomain.com/ws-telemetry
NEXT_PUBLIC_ELMODE_NODE_ID=VEH-001
